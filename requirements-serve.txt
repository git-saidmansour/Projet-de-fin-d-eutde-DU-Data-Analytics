# Lean runtime dependencies for the API + dashboard containers.
# (Excludes collection/training/notebook-only deps like lightgbm, mlflow, jupyter, pytest.)
python-dotenv>=1.0
pandas>=2.2

scikit-learn>=1.4
xgboost>=2.0
shap>=0.45
joblib>=1.3

fastapi>=0.110
uvicorn[standard]>=0.29

streamlit>=1.32
plotly>=5.20
requests>=2.31
